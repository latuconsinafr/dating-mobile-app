/**
 * Defines enum for subscription type.
 */
export enum SubscriptionType {
  /**
   * Defines subscription type for the `no swipe quota for user`.
   */
  UnlimitedSwipe = 'unlimited-swipe',

  /**
   * Defines subscription type for `verified label for user`.
   */
  VerifiedBadge = 'verified-badge',
}
