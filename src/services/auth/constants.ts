// * Default messages section
/**
 * Defines the message when the user has been authenticated.
 */
export const USER_AUTHENTICATED_MESSAGE = 'User authenticated';

/**
 * Defines the message when the user has been signed in.
 */
export const USER_SIGNED_IN_MESSAGE = 'User signed in';

/**
 * Defines the message when the user has been signed up.
 */
export const USER_SIGNED_UP_MESSAGE = 'User signed up';

/**
 * Defines the message when the provider credential is wrong (does not match).
 */
export const WRONG_CREDENTIAL_MESSAGE = 'Wrong credential provided';

/**
 * Defines the message when the request is unauthorized.
 */
export const UNAUTHORIZED_MESSAGE = 'Unauthorized';
